//script que permita escribir los (numero que elija el usuario) primeros numeros pares.
let nPares = prompt("cuantos numeros de pares quieres?")
let nPar = 0

for (let index = 0; index < nPares; index ++){
    console.log(nPar)
    nPar = nPar + 2;
}
